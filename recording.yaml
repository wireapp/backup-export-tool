server:
  applicationConnectors:
    - type: http
      port: 8080

  requestLog:
    appenders: []

logging:
  level: DEBUG
  
auth: ${RECORDING_WIRE_TOKEN}

storage:
  host: localhost
  port: 5432
  database: postgres
  driver: postgresql
  user: admin
  password: ${POSTGRES_PASSWORD}

db:
  host: localhost
  port: 6379 #redis
  user: admin
  password: ${REDIS_PASSWORD}
